age = 34 # integer
name = "Harry" # string 
cgpa = 6.96 # float 

# Rule of defining a variable in python 
# variable names must start with a letter (a-z, A-Z) or an underscore (_).
# They can contain letter, number, and underscores.
# Avoid using python keyword (e.g., if, for, while) as variable names.

# 34age = 4 # Invalid because variable cannot starrt with a number 
age = 32 # valid because variable can start with a number 
# a$$ge = 45 # Invalid because varibles cannot contain special characters other than _
__age = 34
__nice_45 = 34 
a_b_c_7 = "sam"